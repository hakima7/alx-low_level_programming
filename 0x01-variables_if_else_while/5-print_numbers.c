#include <stdlib.h>

#include <time.h>

#include <stdio.h>



/**
 * main - Entry point
 * Return: Always (Success)
 */

int main(void)

{
	int number;

	for (number = 0; number < 10; nimber++)
	putchar(number + '0');
	putchar(('\n');
			return (0);
}
